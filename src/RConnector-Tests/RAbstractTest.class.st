"
A abstract class for the R connector tests.
Seems empty, but load the needed pool dictionaries
"
Class {
	#name : #RAbstractTest,
	#superclass : #TestCase,
	#pools : [
		'RLibTypes'
	],
	#category : #'RConnector-Tests'
}
